const SplashScreen = () => {
    return (
        <div className="fixed inset-0 flex items-center justify-center bg-white z-50">
            <h1 className="text-3xl font-bold">Loading...</h1>
            {/* Or add a logo/spinner here */}
        </div>
    );
};

export default SplashScreen;